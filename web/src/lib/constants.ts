// ── Polling Intervals (milliseconds) ──
export const POLL_INTERVAL_MS = 30000
export const STATUS_CHECK_INTERVAL_MS = 30000

// ── UI Timeouts (milliseconds) ──
export const COPY_TIMEOUT_MS = 2000

// ── Server Defaults ──
export const DEFAULT_SERVER_URL = 'http://192.168.x.x:9527'
