// ── Default Server Configuration ──
export const DEFAULT_SERVER_URL = 'http://localhost:4321'

// ── Polling Intervals (milliseconds) ──
export const POLL_INTERVAL_MS = 3000
export const STATUS_CHECK_INTERVAL_MS = 3000
export const NEW_MESSAGE_CHECK_INTERVAL_MS = 1500
export const REFRESH_INTERVAL_MS = 30000

// ── UI Timeouts (milliseconds) ──
export const TOAST_TIMEOUT_MS = 3000
export const UNDO_TIMEOUT_MS = 5000
export const COPY_TIMEOUT_MS = 2000
export const ANIMATION_TIMEOUT_MS = 500
