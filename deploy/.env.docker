# NotifyHub Docker Configuration

# Nginx port (public-facing, the only exposed port)
NGINX_PORT=80

# Storage path — use an absolute host path for easy backup and migration
DB_PATH=/opt/notifyhub/data

# JWT secret (auto-generated if not set, but set explicitly for production)
# JWT_SECRET=your-jwt-secret-here
